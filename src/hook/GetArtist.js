import React, { useState } from 'react'

const GetArtist = () => {
  const artist= "";
  const searchArtist = [];
  const showModal=false;
  const newMessage = false;
  return [
    artist, searchArtist, showModal, newMessage
  ]
}

export default GetArtist
